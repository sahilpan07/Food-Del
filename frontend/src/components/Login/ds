/* import React, { useState } from 'react'
import './Login.css'
import {assets} from '../../assets/assets'

const Login = ({setShowLogin}) => {

  const [currState,setCurrState] = useState("Login")

  return (
    <div className='fixed top-0 left-0 w-full h-full bg-black-500 flex itms-center justify-center z-50'>
      <form className=" bg-white p-5 rounded w-full max-w-sm box-border relative">
        <div className="flex justify-between items-center mb-5">
          <h2 className='text-xl font-semibold m-0'>{currState}</h2>
          <img className='pointer w-5 h-5' onClick={()=>setShowLogin(false)} src={assets.cross_icon} alt="" />
        </div>
        <div className="flex flex-col gap-5">
          {currState==="Login"?<></>:<input type="text" placeholder='Your Name' required />}
          <input className='border border-gray-500 p-3 rounded text-4 w-full box-border mb-3 focus:border-orange-500 hover:border-orange-600 ' aype="email" placeholder='Your email'  required />
          <input className='border border-gray-500 p-3 rounded text-4 w-full box-border mb-3 focus:border-orange-500 hover:border-orange-600' type="password" placeholder='Your password' required />
        </div>
        <button className='p-2 rounded text-white bg-orange-600 text-4 w-full pointer mb-2'>{currState==="Sign Up"?"Create an Account":"Login"}</button>
        <div className="login-condition">
          <input type="checkbox" required />
          <p>By continuing, i agree  to the terms of use & privacy policy</p>
        </div>
        {currState==="Login"
          ?<p>Create a new Accoont? <span onClick={()=>setCurrState("Sign Up")}>Click Here</span></p>
          :<p>Already Have an Account? <span onClick={()=>setCurrState("Login")}>Login Here</span></p>
        }
      </form>
    </div>
  )
}

export default Login 
 */